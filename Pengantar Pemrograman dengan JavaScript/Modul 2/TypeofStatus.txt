Dalam video ini, Anda akan belajar cara menggunakan jenis operator untuk mengidentifikasi tipe data elemen dalam JavaScript. Jenis operator menerima dan mengevaluasi parameter dan mengembalikan nama tipe data yang direpresentasikan sebagai string. Untuk menggunakannya, Anda dapat mengetik typeof diikuti oleh parameter yang terlampir dalam tanda kurung. Dalam kode saya, perhatikan bahwa saya telah membuat beberapa contoh pengujian variabel. Masing-masing diberi jenis operator dengan parameter yang berbeda. Untuk memeriksa hasilnya, saya juga menggunakan metode log konsol untuk menampilkan nilai pengujian variabel. Oke, jadi seperti yang mungkin Anda perhatikan, saya telah mengomentari semua baris kode yang berisi variabel uji. Untuk menunjukkan jenis operator menggunakan parameter yang berbeda. Saya akan mengabaikan setiap baris ini satu per satu. Jalankan kode dan kemudian periksa outputnya. Pertama., baris 3 yang tidak biasa yang berisi jenis operator melewati parameter string. Ketika saya menjalankan kode, perhatikan bahwa nama tipe data adalah output yang merupakan string. Selanjutnya, izinkan saya baris 5 yang tidak biasa yang berisi nilai angka. Saya menjalankan ulang kode dan memperhatikan bahwa tipe data bernama number adalah output. Jika saya melakukan langkah-langkah ini pada angka desimal saya mendapatkan hasil yang sama untuk angka desimal 3,14. Oke, sekarang mari kita periksa tipe data Boolean benar dan salah. Jika saya menjalankan kode, tipe data bernama Boolean dikeluarkan ke konsol. Saya tidak biasa baris 11, jalankan ulang kode dan perhatikan bahwa Boolean juga merupakan output. Oke, selanjutnya izinkan saya menunjukkan kepada Anda bahwa Anda juga dapat melewati perbandingan karena mereka akan mengevaluasi ke satu nilai. Intinya satu parameter. Misalnya, Anda dapat melewati kondisi seperti 1<2. Ketika saya menjalankan kode, Boolean adalah output. Anda juga dapat meneruskan array sebagai parameter ke jenis operator. Namun, hasilnya mungkin agak mengejutkan. Misalnya, di sini saya memiliki array yang berisi angka 1, 2 dan 3. Jika saya menjalankan kode, tipe data bernama objek adalah output. Ini karena array dalam JavaScript sebenarnya adalah objek. Melewatkan objek melalui tipe operator juga mengembalikan objek. Akhirnya, Anda dapat meneruskan fungsi sebagai parameter dari jenis operator. Jika saya tidak menggunakan baris 19 dan menjalankan kodenya. Perhatikan bahwa fungsi nama tipe data adalah output. Oke, dan itu saja untuk video ini. Anda sekarang tahu tentang jenis operator dan bagaimana itu dapat digunakan dalam JavaScript untuk memeriksa tipe data. Saya mendorong Anda untuk mencobanya dalam kode praktik Anda sendiri.